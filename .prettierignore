node_modules/
package-lock.json
css/tailwind.css
# index.html is intentionally left to hand-formatting: reformatting the
# whole file (markup + inline script together) risks a large, low-value
# diff. Only the standalone .js files are Prettier-formatted.
index.html
